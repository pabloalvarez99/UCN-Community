import React from 'react';
import { Link } from 'react-router-dom';
import { HomeIcon, ArrowLeftIcon } from '@heroicons/react/24/outline';

const NotFound: React.FC = () => {
  return (
    <div className="min-h-screen bg-ucn-gradient flex items-center justify-center px-4 sm:px-6 lg:px-8">
      <div className="max-w-md w-full space-y-8 text-center">
        <div>
          <div className="mx-auto h-24 w-24 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center mb-8">
            <span className="text-4xl font-bold text-white">404</span>
          </div>
          <h2 className="text-3xl font-display font-bold text-white mb-2">
            Página no encontrada
          </h2>
          <p className="text-ucn-100 text-lg mb-8">
            Lo sentimos, no pudimos encontrar la página que buscas.
          </p>
        </div>

        <div className="space-y-4">
          <Link
            to="/dashboard"
            className="btn-secondary w-full py-3 text-white border-white/20 hover:bg-white/10 flex items-center justify-center"
          >
            <HomeIcon className="h-5 w-5 mr-2" />
            Ir al Dashboard
          </Link>
          
          <button
            onClick={() => window.history.back()}
            className="btn-ghost w-full py-3 text-white hover:bg-white/10 flex items-center justify-center"
          >
            <ArrowLeftIcon className="h-5 w-5 mr-2" />
            Volver atrás
          </button>
        </div>

        <div className="text-center pt-8">
          <p className="text-sm text-white/80">
            Universidad Católica del Norte © 2024
          </p>
        </div>
      </div>
    </div>
  );
};

export default NotFound;